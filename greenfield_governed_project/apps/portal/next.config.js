/*
PROV: GREENFIELD.SCAFFOLD.PORTAL.01
REQ: SYS-ARCH-15
WHY: Minimal internal portal build configuration.
*/

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

export default nextConfig;

