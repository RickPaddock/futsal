You are operating in a governed repo.

GOAL: Close intent <INTENT_ID> by validating tasks, provenance, and requirements; then updating canonical sources.

Hard rules:
- Never hand-edit generated .md files.
- Edit only canonical sources: spec/intents/*.json, spec/tasks/*.json, spec/requirements/**.json, spec/md/**/*.mdt, code.
- After changes: run `npm run generate` and ensure `npm run guardrails` passes.

Checklist:
1) Load intent spec: spec/intents/<INTENT_ID>.json
2) Verify every `task_ids_planned[]` has a spec file at spec/tasks/<TASK_ID>.json
3) For each task:
   - Ensure task has concrete deliverables and (when relevant) subtasks.
   - If any subtask_id starts with `REQ-`, ensure that requirement exists in spec/requirements/* with status `todo`.
4) Run intent audit:
   - Command: `npm run audit:intent -- --intent-id <INTENT_ID>`
   - Fix any issues: missing task specs, missing req entries, missing REQ: tags in code, missing PROV/WHY tags.
5) If audit is clean, update canonical sources:
   - Mark completed tasks/subtasks as `done`.
   - For any `REQ-*` requirements created by this intent, set `tracking.implementation` to `done` only if code exists and references it via `REQ: REQ-*`.
   - Update intent `status` to `closed` and add `closed_date` = YYYY-MM-DD.
6) Regenerate + validate:
   - `npm run generate`
   - `npm run guardrails`
7) Record evidence (recommended):
   - `node tools/evidence/record_run.mjs --intent-id <INTENT_ID> --out status/audit/<INTENT_ID>/runs/<run_id>/run.json -- npm run audit:intent -- --intent-id <INTENT_ID>`
